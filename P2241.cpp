#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
// #define int long long

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    for
    for
     if (i==j) {
         a1+=(n-i+1)*(m-j+1);
     } else {
         a2+=同上;
     }
    cout << a1 << " " << a2;
    // 优化
    return 0;
}
