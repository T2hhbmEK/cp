#include<bits/stdc++.h>
using namespace std;
int main() {
  int r = 0;  // 0
//   cin >> r;
  if (r < 0) {  // 0
    r = -r;
  }
  double pi = 3.14;  // 3.14
  cout << "C=2*pi*r=" <<  // "xx"
    2*pi*r << endl;  // 2
  size_t np = -1;
  cout << np << endl;
  cout << ULONG_LONG_MAX << endl;
  cout << (np == -1) << endl;
  return 0;
}