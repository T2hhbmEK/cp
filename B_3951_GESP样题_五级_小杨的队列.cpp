#include <bits/stdc++.h>
using namespace std;

const int N = 2e3 + 10;
int n, a[N], m, x;

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    cin >> m;
    while(m--) {
        cin >> x;
    }
    return 0;
}
